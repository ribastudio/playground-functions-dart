void main() {
  List listaProdutos = [1, 2, 3, 4, 'rua', 6, 7, 8, 9, 10];

  print(listaProdutos[4]);

  Map lista_maps = {
    'chave': 'valor',
    'chave2': 'valor2',
    'idade': 24,
    'verificado': true,
  };

  print(lista_maps['idade']);
  print(lista_maps.length);
}
// criar variavel tipada: String 'nome variavel' = 'Stefane';